# End-to-End Tests

This directory contains end-to-end tests that verify behaviour across the
entire platform.

These tests exercise complete user journeys rather than individual
applications.

Initially this directory will remain empty until the first vertical slice of
the platform is complete.
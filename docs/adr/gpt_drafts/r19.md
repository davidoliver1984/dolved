The architectural amendments look good and I do not have another substantive
design change.

However, I noticed an important process issue in your report:

you state that ADR-0019 is shown as modified because it had already been
committed as Accepted.

If ADR-0019 is already committed/tagged as Accepted, we must preserve the
repository's accepted-ADR immutability rule.

Do not rewrite/re-accept ADR-0019 in place.

Please first verify the Git history and tag state for R16-S05 / ADR-0019.

If ADR-0019 was indeed committed as Accepted, then:

1. restore ADR-0019 exactly to its accepted committed contents;
2. create a narrow follow-up ADR:

# ADR 0020: Clarify Retrieval Evaluation Evidence and Model-Assisted Evaluation Semantics

or recommend a better concise title if appropriate.

ADR-0020 should NOT redesign ADR-0019.

It should explicitly state that ADR-0019 remains authoritative except for the
bounded clarifications discovered during Codex's pre-R16-S06 implementation review.

The follow-up ADR should capture only:

- the corrected adversarial-case ownership:
  - adversarial user questions -> planner;
  - adversarial document content -> retrieval/ranking;
  - document-content prompt injection affecting answers -> Phase 17 generation
    evaluation;

- the repository-owned EvidenceUnit contract:
  - evidence_id;
  - Document/DocumentFamily/version identity;
  - one or more canonical source excerpts;
  - relevance grade;
  - deterministic matching/coverage requirement;
  - multi-chunk combined coverage;
  - distinct evidence-unit semantics for Recall/Precision/MRR/nDCG;

- deterministic duplicate-credit handling being required, versioned and recorded,
  while its exact formula remains R16-S06 implementation detail;

- ModelAssistedEvaluator -> RagasEvaluator remaining the V1 architecture;

- RagasEvaluator receiving an injected, configuration-owned evaluator model/client;

- ordinary tests requiring no credentials/network calls:
  deterministic fake evaluator + fake injected judge client;

- live RAGAS/provider testing being explicit, credential-dependent and opt-in;

- model-assisted failure being advisory and incapable of erasing deterministic
  results or hard-gate outcomes;

- Phase-16 ModelAssistedEvaluationRequest remaining retrieval-specific and not
  containing speculative generated/reference-answer fields;

- RAGAS context relevance being an aggregate context-set judgement, never
  candidate-level attribution;

- V1 access/isolation cases matching the permission model actually implemented;

- canonical corpus/policy content digests being part of experiment/baseline
  lineage;

- case-first aggregation of phrasing variants;

- deterministic metric reproducibility being distinct from stochastic live-model
  reproducibility, with model trial/configuration/variance lineage where relevant;

- R16-S06 remaining the implementation owner for all of the above.

ADR-0020 should explicitly say it clarifies/supersedes ADR-0019 in part only for
these points.

Do not modify ADR-0019 if it is already accepted and committed.

Do not implement R16-S06 yet.

Before changing roadmap/guide/tasks, draft ADR-0020 as Proposed and report:

1. confirmed Git/ADR-0019 acceptance state;
2. exact partial-supersession scope;
3. final EvidenceUnit semantics;
4. final RAGAS/evaluator testing semantics;
5. final deterministic-versus-stochastic reproducibility semantics;
6. whether any part of the current amendment cannot cleanly be expressed as a
   narrow clarification to ADR-0019;
7. confirmation that accepted ADR-0019 is byte-identical to its committed form.

Stop for review.

If, contrary to the report, ADR-0019 was never actually committed/tagged as
Accepted, report that instead and do not create ADR-0020 yet.